#include <stdio.h>

/**
 * main - print number from 0 to 10
 * Return: If there is no problem should be 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
