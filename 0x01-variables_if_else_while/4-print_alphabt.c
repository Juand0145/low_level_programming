#include <stdio.h>

/**
 * main - Print alphabet exept q and e
 * Return: If there is no porble should be 0
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
