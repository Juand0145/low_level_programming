#include <stdio.h>

/**
 * main - print number from 0 to 10
 * Return: if tehre is no proble should be 0
 */
int main(void)
{
	int n;

	for (n = 0, n <= 10, n++)
		printf("%d", n);
	printf('\n');

	return (0);
}
